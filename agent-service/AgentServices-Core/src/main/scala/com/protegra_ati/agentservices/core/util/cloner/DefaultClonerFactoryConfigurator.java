package com.protegra_ati.agentservices.core.util.cloner;

import com.rits.cloning.Cloner;

public class DefaultClonerFactoryConfigurator implements ClonerFactoryConfigurator
{
    public void configure( final Cloner toBeConfigurated ) {
      //don't apply any possible settings for cloner
    }
}
